#include <stdio.h>
#include <string.h>

int main(){
    char str[100];
    fgets(str,100,stdin);

    if(strstr(str,"if") && strstr(str,"else"))
        printf("Valid if-else\n");
    else
        printf("Invalid\n");
}

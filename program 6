#include <stdio.h>

int main(){
    char str[100];
    int op=0;

    scanf("%s",str);

    for(int i=0;str[i];i++){
        if(str[i]=='+'||str[i]=='*') op++;
    }

    if(op>1) printf("Possibly Ambiguous\n");
    else printf("Not Ambiguous\n");
}
